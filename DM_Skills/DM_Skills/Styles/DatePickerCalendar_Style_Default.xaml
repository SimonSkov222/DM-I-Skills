<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DM_Skills.Styles">

    <Style  TargetType="{x:Type CalendarItem}">
        <Setter Property="Margin" Value="50"/>
        <Setter Property="Template" Value="{StaticResource Calendar_Template_Default}"/>
    </Style>

    <Style x:Key="DatePickerCalendar_Style_Default" TargetType="{x:Type Calendar}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderBrush" Value="{StaticResource Color_Border_InputBox}" />
        <Setter Property="BorderThickness" Value="1.5" />

        <Setter Property="Template" Value="{StaticResource DatePickerCalendar_Template_Default}" />
    </Style>

</ResourceDictionary>