<Window x:Class="DM_Skills.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DM_Skills"
        xmlns:Controls="clr-namespace:DM_Skills.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <ScrollViewer>
        <Grid HorizontalAlignment="Center" Margin="0 15">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <Controls:TablesControl Margin="0 0 0 0" FontSize="16" Title="Bord 1" Panel.ZIndex="1005"/>
                <Controls:TablesControl Margin="0 10 0 0" FontSize="16" Title="Bord 2" Panel.ZIndex="1004"/>
                <Controls:TablesControl Margin="0 10 0 0" FontSize="16" Title="Bord 3" Panel.ZIndex="1003"/>
                <Controls:TablesControl Margin="0 10 0 0" FontSize="16" Title="Bord 4" Panel.ZIndex="1002"/>
                <Controls:TablesControl Margin="0 10 0 0" FontSize="16" Title="Bord 5" Panel.ZIndex="1001"/>
            </StackPanel>

            <Grid Grid.Row="0" Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="#" Foreground="Transparent" Grid.Row="0" FontSize="16"/>
                <Button Grid.Row="1" Content="Indsend" Foreground="White" Background="CadetBlue" FontSize="20" Padding="15 5" FontWeight="Bold" VerticalAlignment="Top"/>
                <Button Grid.Row="2" Content="Nulstilling af rounde" Margin="0 10 0 0" Padding="15 0" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                <TextBlock  Grid.Row="3" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 5">
                    #<Run Text="1"/>
                </TextBlock>
                <Controls:DragAndDropTimerControl x:Name="LapList" Grid.Row="4" BorderThickness="2" BorderBrush="Black" Background="LightGray" Height="250" VerticalAlignment="Bottom"/>
            </Grid>

            <Controls:TimerControl Grid.Row="1" Grid.ColumnSpan="3" Margin="0 25 0 0" OnLap="TimerControl_OnLap" OnReset="TimerControl_OnReset"/>
        </Grid>
    </ScrollViewer>
</Window>
